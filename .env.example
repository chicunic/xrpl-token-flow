# XRPL Configuration
# Available networks: mainnet | testnet | devnet | local
XRPL_NETWORK=devnet

# Fund wallet secret (not needed for local network, which uses the genesis account)
FUND_SECRET=sEdxxxxxxxxxxxxxxxxxxxxxxxx
